language: cpp

# need at least gcc 4.8 for -std=c++11
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
    - g++-5

# use gcc-4.8
install:
- if [ "$CXX" = "g++" ]; then export CXX="g++-5" CC="gcc-5"; fi

script: make

compiler:
- clang
- gcc
